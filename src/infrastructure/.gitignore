.terraform/
# .terraform.lock.hcl is intentionally tracked so CI uses pinned provider versions
terraform.tfstate
terraform.tfstate.backup
terraform.tfvars
*.auto.tfvars
crash.log
override.tf
override.tf.json
